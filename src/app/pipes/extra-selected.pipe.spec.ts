import { ExtraSelectedPipe } from '../pipes/extra-selected.pipe';

describe('ExtraSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtraSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
